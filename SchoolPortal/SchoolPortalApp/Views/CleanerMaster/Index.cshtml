@model IEnumerable<SchoolPortalApp.Models.CleanerListItemViewModel>
@{
    ViewData["Title"] = "Cleaners";
}
<h2>Cleaners</h2>
<p>
    <a class="btn btn-primary" asp-controller="CleanerMaster" asp-action="Create">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Father Name</th>
            <th>Is Active</th>
            <th>School</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model)
{
    <tr>
        <td>@item.Name</td>
        <td>@item.FatherName</td>
        <td>@item.IsActive</td>
        <td>@item.SchoolName</td>
        <td>
                    <div class="btn-group btn-group-sm">
                        <a asp-controller="Emp" asp-action="Details" asp-route-id="@item.Id" class="btn btn-info" title="View Details">
                            <i class="bi bi-eye"></i>
                        </a>
                        <a asp-controller="Emp" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning" title="Edit">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <a asp-controller="Emp" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" title="Delete">
                            <i class="bi bi-trash"></i>
                        </a>
                    </div>
        </td>
    </tr>
}
    </tbody>
</table>
